services:
  backend:
    build: ./backend
    restart: unless-stopped
    volumes:
      - uploads:/app/uploads
      - dbdata:/app/data

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "8090:80"
    depends_on:
      - backend

volumes:
  uploads:
  dbdata:
